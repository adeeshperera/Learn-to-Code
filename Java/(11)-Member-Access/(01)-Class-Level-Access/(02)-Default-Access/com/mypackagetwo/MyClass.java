package com.mypackagetwo; // Package declaration

// Class has default access (no public keyword), so it is accessible only within this package
class MyClass {
    // Public method that returns a greeting message
    public String sayHello() {
        return "hello";
    }
}
