import React from "react";

function FragmentDemo() {
	return (
		// This can be used if we want to remove the dev tag
		<React.Fragment>
			<h1>Fragment Demo</h1>
			<p>This describes the Fragment Demo component</p>
		</React.Fragment>
	);
}

export default FragmentDemo;
