import React from "react";

function Columns() {
	return (
		// It can accept the key attribute when rendering lists of items Eg.- <React.Fragment key={item.id}>
		<>
			{/* <React.Fragment> = <> */}
			<td>Name</td>
			<td>Vishwas</td>
		</>
	);
}

export default Columns;
